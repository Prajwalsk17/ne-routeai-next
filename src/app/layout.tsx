import type { Metadata } from 'next';
import { Inter, JetBrains_Mono } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  variable: '--font-mono',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'NER-RouteAI — AI Logistics Intelligence for Northeast India',
  description: 'AI-Powered Smart Logistics & Accessibility Intelligence for the Northeast Region. Predict disruptions. Optimize routes. Deliver without interruption.',
  keywords: ['logistics', 'northeast india', 'AI', 'route optimization', 'accessibility', 'disaster management'],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable}`}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
